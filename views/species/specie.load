<html>
    <head>

    </head>

    <body>
        <div class="info_box">
            <h5>{{=specie.name}}</h5>
                {{ for a in assembled_seq:}}
                    <div class="specie_box">
                        <a href="{{=URL(c="assemblies",f="assembly",args=[a.id,], extension=False)}}">{{=a.name}}</a>
                    </div>
                {{ pass }}
            </div>
    </body>
</html>