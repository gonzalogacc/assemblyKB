
<div style="border: 2px lightblue solid; padding-top: 5px; padding-left: 10px; padding-bottom: 5px; padding-right: 10px" id="Assembly data">
    <h4>Assembly settings for {{=assembly.name}}</h4>
    <div style="border: 2px lightblue solid; padding-top: 5px; padding-left: 10px; padding-bottom: 5px; padding-right: 10px" >
            <p>Specie: <a href="{{=URL(c="species",f="specie",args=[assembly.specie,], extension=False)}}">{{=specie}}</a></p>
            <p>Dataset: <a href="{{=URL(c="datasets",f="dataset",args=[assembly.dataset,], extension=False)}}">{{=dataset}}</a></p>
            <p>Assembly run: <a href="{{=URL(c="assemblies_run",f="assembly_run",args=[assembly.assembly_run,], extension=False)}}">{{=assembly_run}}</a></p>
    </div>
</div>
